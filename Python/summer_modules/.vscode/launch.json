{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "SummerModules单文件脚本调试",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            }
        }
    ]
}